// TypeScript Configuration file setup
// 1: Enter command "tsc --init" and hit enter.
// 2: Before that we need to compile ts file with command "tsc app.ts" now it is all defined in below config file automatically.